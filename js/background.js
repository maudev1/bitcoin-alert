// chrome.browserAction.onClicked.addListener(clickButton);
chrome.browserAction.onClicked.addListener(clickButton)
function clickButton (teste) {

    console.log(chrome.popup)

    //chrome.tabs.sendMessage()
    
}
